/* Tema Light */
body.light-theme {
    background-color: #f7f7f7; /* Sfondo chiaro simile a iOS */
    color: #000000; /* Testo nero */
    font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
    margin: 0;
    padding: 0;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
}

/* Tema Dark */
body.dark-theme {
    background-color: #000000; /* Sfondo nero */
    color: #ffffff; /* Testo bianco */
    font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
    margin: 0;
    padding: 0;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
}

/* Contenitore principale che racchiude il corpo centrale */
.container {
    width: 100%;
    max-width: 1024px;
    margin: 0 auto;
    padding: 20px;
    box-sizing: border-box;
}

/* Pulsante del tema (moon icon) */
.theme-toggle {
    background: none;
    border: none;
    font-size: 24px;
    color: inherit;
    cursor: pointer;
    position: absolute;
    top: 20px;
    right: 20px;
    padding: 8px;
}

/* Pulsanti di navigazione */
button.main, button.start, button.back, button.google-maps-button, button.home-button {
    font-size: 16px;
    padding: 12px 24px;
    border: none;
    border-radius: 12px; /* Angoli arrotondati */
    cursor: pointer;
    text-align: center;
    width: auto;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1); /* Ombra per i pulsanti, stile iOS */
    transition: all 0.3s ease;
}

/* Tema Light: Pulsanti */
body.light-theme button.main {
    background-color: #34c759; /* Verde per navigazione */
    color: white;
}

body.light-theme button.main:hover {
    background-color: #28a745; /* Verde più scuro per hover */
    box-shadow: 0 6px 10px rgba(0, 0, 0, 0.15); /* Ombra più accentuata */
}

body.light-theme button.back {
    background-color: #ff3b30; /* Rosso per il tasto indietro */
    color: white;
}

body.light-theme button.back:hover {
    background-color: #d93d2a; /* Rosso più scuro per hover */
    box-shadow: 0 6px 10px rgba(0, 0, 0, 0.15); /* Ombra più accentuata */
}

body.light-theme button.google-maps-button {
    background-color: #ff9500; /* Arancione per Google Maps */
    color: white;
}

body.light-theme button.google-maps-button:hover {
    background-color: #e67900; /* Arancione più scuro per hover */
    box-shadow: 0 6px 10px rgba(0, 0, 0, 0.15); /* Ombra più accentuata */
}

body.light-theme button.home-button {
    background-color: #007aff; /* Blu per torna alla home */
    color: white;
}

body.light-theme button.home-button:hover {
    background-color: #0051a8; /* Blu più scuro per hover */
    box-shadow: 0 6px 10px rgba(0, 0, 0, 0.15); /* Ombra più accentuata */
}

/* Tema Dark: Pulsanti */
body.dark-theme button.main {
    background-color: #9b59b6; /* Viola per navigazione */
    color: white;
}

body.dark-theme button.main:hover {
    background-color: #8e44ad; /* Viola più scuro per hover */
    box-shadow: 0 6px 10px rgba(0, 0, 0, 0.15); /* Ombra più accentuata */
}

body.dark-theme button.back {
    background-color: #ff9500; /* Arancione per il tasto indietro */
    color: white;
}

body.dark-theme button.back:hover {
    background-color: #e67900; /* Arancione più scuro per hover */
    box-shadow: 0 6px 10px rgba(0, 0, 0, 0.15); /* Ombra più accentuata */
}

body.dark-theme button.google-maps-button {
    background-color: #34c759; /* Verde per Google Maps */
    color: white;
}

body.dark-theme button.google-maps-button:hover {
    background-color: #28a745; /* Verde più scuro per hover */
    box-shadow: 0 6px 10px rgba(0, 0, 0, 0.15); /* Ombra più accentuata */
}

body.dark-theme button.home-button {
    background-color: #ffcc00; /* Giallo per torna alla home */
    color: white;
}

body.dark-theme button.home-button:hover {
    background-color: #f5b700; /* Giallo più scuro per hover */
    box-shadow: 0 6px 10px rgba(0, 0, 0, 0.15); /* Ombra più accentuata */
}

/* Layout per i pulsanti */
.button-container {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 15px;
    margin-top: 20px;
}

.itinerary-buttons {
    display: flex;
    gap: 10px;
    justify-content: space-between; /* Pulsanti affiancati */
    width: 100%;
}

.back-home-buttons {
    display: flex;
    justify-content: space-between;
    width: 100%;
    position: absolute;
    bottom: 20px;
    left: 0;
    right: 0;
    padding: 0 20px;
}

/* Pulsanti fissi in fondo alla pagina */
.back-home-buttons button {
    width: 48%; /* Pulsanti alla stessa larghezza */
}

/* Media queries per la visualizzazione mobile */
@media screen and (max-width: 768px) {
    .container {
        padding: 10px;
    }

    button.main,
    button.start,
    button.back,
    button.google-maps-button,
    button.home-button {
        width: auto; /* Pulsanti non full-width */
    }

    .itinerary-buttons {
        flex-direction: row;
        gap: 10px;
    }

    .back-home-buttons {
        position: fixed;
        bottom: 20px;
        left: 0;
        right: 0;
        padding: 0 20px;
    }
}
