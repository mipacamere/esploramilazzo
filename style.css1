/* Body e impostazioni generali */
body {
    font-family: Arial, sans-serif;
    margin: 0;
    padding: 0;
    background-color: #f4f4f4;
    color: #333;
    line-height: 1.6;
    display: flex;
    justify-content: center;
    align-items: flex-start;  /* Allinea il contenuto verso l'alto */
}

/* Contenitore del pulsante "Torna alla Home" allineato a destra */
.right-button-container {
    display: flex;
    justify-content: flex-end; /* Allinea il bottone a destra */
    margin-top: 20px; /* Distanza tra il pulsante "Inizia il tuo viaggio" e "Torna alla Home" */
}

.right-button-container a {
    text-decoration: none; /* Rimuove la sottolineatura del link */
}

/* Stile personalizzato per il pulsante "Torna alla Home" */
.right-button-container button {
    background-color: #007bff; /* Colore blu per il bottone */
    color: #ffffff; /* Colore del testo bianco per contrasto */
    padding: 15px 30px;
    border: none;
    border-radius: 8px;
    font-size: 1.1em;
    cursor: pointer;
    transition: all 0.3s ease;
}

/* Effetto hover per il pulsante "Torna alla Home" */
.right-button-container button:hover {
    background-color: #0056b3; /* Colore blu più scuro al passaggio del mouse */
}

/* Contenitore principale */
.container {
    max-width: 1100px;
    margin: 50px auto;
    padding: 40px;
    background-color: #ffffff; /* Sfondo bianco per il contenitore */
    border-radius: 20px; /* Bordo arrotondato */
    box-shadow: 0 10px 40px rgba(0, 0, 0, 0.1); /* Ombra leggera per profondità */
    text-align: center;
}

/* Titoli */
h1, h2 {
    color: #333; /* Colore scuro per i titoli */
    font-weight: 700; /* Più enfasi sui titoli */
    text-transform: uppercase; /* Testo maiuscolo per un effetto distintivo */
    letter-spacing: 2px; /* Spaziatura tra le lettere per un tocco moderno */
    margin-bottom: 40px;
}

h1 {
    font-size: 2rem;
    color: #444;
}

/* Paragrafi */
p {
    font-size: 1.1em;
    color: #555; /* Colore del testo per i paragrafi */
}

/* Stile per i bottoni */
button {
    padding: 15px 30px;
    margin: 15px;
    border: none;
    border-radius: 8px;
    cursor: pointer;
    font-size: 1.1em;
    background-color: #28a745; /* Verde per i bottoni principali */
    color: #ffffff; /* Testo bianco per contrasto */
    transition: all 0.3s ease;
}

/* Effetto hover per tutti i bottoni */
button:hover {
    background-color: #218838; /* Verde più scuro al passaggio del mouse */
}

/* Bottoni principali */
button.main {
    background-color: #28a745; /* Verde per il bottone principale */
    color: #ffffff;
}

button.main:hover {
    background-color: #218838; /* Colore verde scuro al passaggio del mouse */
}

/* Bottoni "Indietro" e "Torna all'inizio" */
button.back, button.start {
    font-size: 0.9em;
    color: #ffffff;
}

button.back {
    background-color: #dc3545; /* Rosso per il bottone "Indietro" */
}

button.back:hover {
    background-color: #c82333; /* Colore rosso scuro al passaggio del mouse */
}

button.start {
    background-color: #007bff; /* Blu per il bottone "Torna all'inizio" */
}

button.start:hover {
    background-color: #0056b3; /* Colore blu scuro al passaggio del mouse */
}

/* Stile per il pulsante di Google Maps */
.google-maps-button {
    background-color: #ff4081; /* Colore neon rosa acceso */
    color: white;
    padding: 15px 30px;
    border-radius: 8px;
    font-size: 18px;
    display: flex;
    align-items: center;
    cursor: pointer;
    text-decoration: none;
    transition: background-color 0.3s ease, transform 0.2s ease;
}

.google-maps-button i {
    margin-right: 12px; /* Più spazio tra l'icona e il testo */
}

.google-maps-button:hover {
    background-color: #ff80ab; /* Colore più chiaro per l'effetto hover */
    transform: scale(1.05);
}

/* Posizionamento dei tasti Indietro e Torna all'inizio */
.buttons-container {
    display: flex;
    justify-content: space-between;
    width: 100%;
}

/* Nascondi gli elementi */
.hidden {
    display: none;
}

/* Aggiungi un effetto smooth per le transizioni */
* {
    transition: all 0.3s ease-in-out;
}

/* Language selection container */
.language-selection {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 20px; /* Distanza tra le bandiere */
    margin-top: 50px;
}

/* Language item (bandiera) */
.language-item {
    cursor: pointer;
    text-align: center;
    width: 80px; /* Impostiamo una larghezza fissa più piccola per ogni elemento */
    transition: transform 0.3s ease-in-out;
}

.language-item:hover {
    transform: scale(1.1); /* Effetto di ingrandimento al passaggio del mouse */
}

.language-item img {
    width: 50px !important; /* Ridotto a 50px per le immagini delle bandiere */
    height: 50px !important; /* Altezza coerente per mantenere proporzioni uniformi */
    border-radius: 5px;
    margin-bottom: 5px; /* Spazio più ridotto sotto le immagini */
}

/* Collegamento (link) */
.language-item a {
    display: block;
    text-decoration: none; /* Rimuove la sottolineatura del link */
}

.language-item p {
    font-size: 0.8rem; /* Ridurre la dimensione del testo */
    color: #555;
    margin-top: 5px; /* Spazio ridotto tra l'immagine e il testo */
}

/* Responsive design */
@media (max-width: 600px) {
    .language-item {
        width: 60px; /* Larghezza più piccola per dispositivi mobili */
    }

    .language-item img {
        width: 40px; /* Immagini più piccole per schermi piccoli */
        height: 40px; /* Altezza coerente per mantenere la proporzione */
    }

    .container {
        padding: 20px;
    }

    h1 {
        font-size: 1.5rem;
    }
}

/* Responsive design per schermi ancora più piccoli */
@media (max-width: 480px) {
    .language-item {
        width: 50px; /* Ridotto ulteriormente la larghezza per schermi molto piccoli */
    }

    .language-item img {
        width: 30px; /* Immagini molto più piccole per schermi piccoli */
        height: 30px; /* Altezza proporzionata */
    }

    .container {
        padding: 10px;
    }

    h1 {
        font-size: 1.2rem; /* Ancora più piccolo per dispositivi molto piccoli */
    }
}
